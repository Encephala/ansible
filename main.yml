---
- name: Main
  hosts: all

  become: true

  vars_files:
    secrets.yml

  vars:
    # gotify_token: "{{ gotify_x_token }}"
    # is_vm: true
    # hostname: Dockerhost

    # Initial login for containers
    # ansible_user: root

  roles:
    - provision
