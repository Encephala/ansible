---
exclude_paths:
  # Don't scan playbooks as those don't work due to password file
  - main.yml
  - playbooks/
  - roles/dependencies/
  - .github/
  - secrets.yml

skip_list:
  - latest

profile: shared
